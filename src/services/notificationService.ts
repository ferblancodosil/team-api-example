export const notifyPlayer = (playerEmail: string, message: string) => {
    // Lógica para enviar un email (puedes usar nodemailer o cualquier otra librería)
    console.log(`Sending email to ${playerEmail}: ${message}`);
};